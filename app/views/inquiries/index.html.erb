<% provide(:title, 'お問合せ一覧') %>
<h1>お問合せ一覧</h1>

<div class="row">
  <div class="span5">
    <% if @inquiries.any? %>
      <ul class="inquiries">
        <%= render partial: 'inquiry_item', collection: @inquiries %>
      </ul>
    <% end %>
    
    <%= will_paginate @inquiries %>
  </div>
  <div class="span7"></div>
</div>

